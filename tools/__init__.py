"""Zona do colaborador — uma tool por arquivo.

Cada arquivo expoe uma variavel `TOOL` (instancia de agent.tool_base.AgentTool).
O auto-discovery (agent/tool_registry.py) registra tudo automaticamente.
Arquivos comecando com `_` sao ignorados. Veja docs/adding-a-tool.md.
"""
